# Panchi – MVP

India-first travel companion MVP.

Routes:
- `/` : basic home page with gradient hero and sample Delhi → Goa plan
- `/api/mockFlights` : returns mock flight options for DEL → GOI
- `/api/mockCabs` : returns mock cab options (Local, Ola, Uber)

Next.js 14 + React 18. Deploy on Vercel.
